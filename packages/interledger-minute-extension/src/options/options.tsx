import ReactDOM from 'react-dom'
import React from 'react'

const rootEl = document.getElementById('root')
const Index = () => {
  return <h1> MINUTE EXTENSION OPTIONS PAGE </h1>
}

ReactDOM.render(<Index />, rootEl)
